<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amjad.amjadgithubuser">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AmjadGithubUser">
        <activity
            android:name=".ui.menu.SettingActivity"
            android:label="Setting Menu"
            android:exported="false" />
        <activity
            android:name=".ui.menu.FavoriteActivity"
            android:label="Favorite Users"
            android:exported="false" />
        <activity
            android:name=".SplasScreenActivity"
            android:exported="true"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.detail.DetailUserActivity"
            android:label="User Detail"
            android:exported="false" />
        <activity
            android:name=".ui.main.MainActivity"
            android:label="Github Users"
            android:exported="false" />
    </application>

</manifest>